openai>=1.50.0
python-dotenv>=1.0.1
requests>=2.32.0
aiohttp>=3.10.0
rich>=13.8.0
pydantic>=2.9.0
PyYAML>=6.0.1
fastapi>=0.115.0
uvicorn>=0.30.0
# Formulários (Form Data) usados pelo FastAPI em alguns fluxos
python-multipart>=0.0.12
# Segurança das API Keys dos usuários (criptografia em repouso)
cryptography>=42.0.0
# Supabase (nícleo de dados/auth opcional — sem as variáveis, o NEMO usa o
# armazenamento local em _data/)
supabase>=2.10.0
